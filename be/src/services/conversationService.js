const Conversation = require("../models/conversationModel");

const findOrCreateConversation = async (userId, receiverId) => {
  return conversation;
};

const getConversation = (module.exports = { findOrCreateConversation });
